# Experiment A1: (N, l) = (2^20, 256B)
# NOTE: mpc4j uses entry_bit_length (unit = bit), so 256B -> 2048 bits.
server_name = server
server_ip = 127.0.0.1
server_port = 9002

client_name = client
client_ip = 127.0.0.1
client_port = 9003

append_string = exp_len_256B_n20
pto_type = SINGLE_CP_KS_PIR

entry_bit_length = 2048
server_log_set_size = 20
query_num = 10
parallel = true

single_cp_ks_pir_pto_name = PGM_INDEX
